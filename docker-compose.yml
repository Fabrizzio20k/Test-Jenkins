services:
  fastapi-app:
    build: .
    container_name: test-jenkins-api
    environment:
      - KEY1=${KEY1}
    networks:
      - proxy_net
    restart: unless-stopped

networks:
  proxy_net:
    external: true